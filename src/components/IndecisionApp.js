import React from 'react';
import Header from './Header';

const IndecisionApp = () => {
    return (
        <div>
            <div>
                <Header />
            </div>
        </div>
    );
};

export default IndecisionApp;
